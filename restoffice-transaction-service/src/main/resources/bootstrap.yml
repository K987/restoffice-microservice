server:
  port: 0
spring:
  application:
    name: restoffice-transaction-service
  profiles:
    active:
    - development
  cloud:
    config:
      discovery:
        enabled: true
        service-id: restoffice-config-server
      fail-fast: false
eureka:
  client:
#    serviceUrl:
#      defaultZone: http://localhost:8761/eureka/
    fetch-registry: true
#    register-with-eureka: true
#    enabled: true